import numpy as np

arr=np.arange(3,18,2) #similar  to range func
print(arr)

arr2=np.linspace(-10,10,5) #generates an array of elements in a give range in which elements are linearly spaced
print(arr2)